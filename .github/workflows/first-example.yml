name: my first worklfow
on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: welcome message
        run: echo "my first github actions job"
      - name: list the files
        run: ls
      - name: read the file
        run: cat varunraikar056/githubactions-new/README.md